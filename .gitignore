# ==================================
#  .gitignore for Voikan Records
# ==================================

# --- Python кеш та артефакти ---
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Віртуальне середовище ---
venv/
.venv/

# --- Артефакти збірки (PyInstaller) ---
*.spec
build/
dist/

# --- Специфічні папки та файли Voikan Records ---

# Папки з кешем програми. НІКОЛИ не додавати в Git.
_cache/

# Папки з секретними даними. НАДЗВИЧАЙНО ВАЖЛИВО ігнорувати.
credentials/
tokens/

# Тимчасові папки, які ти створював (якщо вони ще є)
Нова папка/
Резерв/

# Файли баз даних та серіалізовані об'єкти
*.db
*.sqlite3
*.pkl
*.pickle

# Файли конфігурації, які генеруються автоматично або містять локальні налаштування.
# УВАГА: Якщо якийсь .json є ШАБЛОНОМ, його треба додати примусово.
*.json
# Додаємо винятки для JSON-файлів, які є частиною коду, а не даними
!gpt_cache.json
!ui/pages/gpt/gpt_cache.json

# Виняток для файлів, які можуть помилково потрапити під правило вище
# Наприклад, якщо у тебе є важливий файл конфігурації
# !config_template.json

# --- Файли IDE (Visual Studio Code, PyCharm) ---
.vscode/
.idea/
push_updates.bat
requirements-heavy.txt
_version.txt